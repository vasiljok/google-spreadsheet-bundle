imports:
    - { resource: parameters.yml }

framework:
    secret: "%secret%"

wk_google_spreadsheet:
    credentials:
        token: "%google_spreadsheet_token%"
        private_key: "%kernel.root_dir%/App/private.key%"
